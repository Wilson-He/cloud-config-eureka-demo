spring:
  cloud:
    config:
      name: test
      discovery:
        service-id: cloud-server
        enabled: true
      profile: dev
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:20000/eureka/