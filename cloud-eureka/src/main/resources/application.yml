spring:
  application:
    name: cloud-eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:20000/eureka/
    fetch-registry: false
    register-with-eureka: false
server:
  port: 20000